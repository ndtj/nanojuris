# Clarify

- `blocked_access` é distinto de `empty` e não deve gerar `SearchPage(total=0)`.
- A fotografia não confirma disponibilidade futura nem o contrato de detalhe.
- Nenhum CAPTCHA, login, WAF ou limite foi contornado.
