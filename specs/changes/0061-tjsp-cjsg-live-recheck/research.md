# Pesquisa - TJSP/CJSG ciclo 11

A rota oficial observada é `https://esaj.tjsp.jus.br/cjsg/resultadoCompleta.do`.
O POST público com consulta curta retornou `AccessControlRequiredError` e os
sinais `has_search_form`, `has_recaptcha_field`, `has_uuid_captcha_field`,
`has_access_control_route` e `has_login_script`. Isso não prova ausência de
jurisprudência nem autoriza contorno do controle.
