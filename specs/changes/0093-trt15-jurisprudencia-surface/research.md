# Pesquisa — TRT15

- Entrada oficial: `https://jurisprudencia.trt15.jus.br/`.
- Configuração pública: `/assets/app-config.json`.
- Backend declarado no bundle público: `/backend/`.
- Catálogo: `GET /backend/listarOpcoes`.
- Pesquisa: `POST /backend/pesquisar`.
- Detalhe: `POST /backend/visualizarDocumento`.

A rota foi comparada diretamente com o bundle entregue pela fonte. Nenhum
código de terceiros foi copiado. A resposta `sucesso=3` foi preservada como
controle de acesso.
