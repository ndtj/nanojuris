# Design — acesso público

O transporte terá `AccessPath` e `AccessAttempt`, classificação de desafio,
allowlist de host, limites de bytes, retry transitório e evidência redigida.
Chromium padrão pode executar JavaScript público não interativo. Tokens e
cookies não saem da sessão; solver, stealth e proxy evasivo não existem no API.

