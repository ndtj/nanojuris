# Clarifications

- The catalog operation is metadata discovery for jurisprudence filters, not a
  procedural case lookup.
- A catalog response with an unknown shape is a contract failure, not an empty
  catalog.
