# Clarify

- `blocked_access` representa o desafio observado na pagina inicial; nao e
  `empty` e nao deve gerar `SearchPage(total=0)`.
- O registro e uma fotografia de ambiente e nao garante disponibilidade futura.
- Nenhum CAPTCHA, WAF, rate limit ou controle de acesso foi contornado.
