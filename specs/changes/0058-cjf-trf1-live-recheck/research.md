# Pesquisa - CJF/TRF1 ciclo 8

A rota oficial observada foi `https://jurisprudencia.cjf.jus.br/trf1/index.xhtml`.
Com User-Agent padrao e TLS habilitado, respondeu HTTP 200 e
`text/html;charset=UTF-8`, mas o documento continha `captcha` e `recaptcha` e
nao continha tabela `table_resultado`. Isso e controle de acesso, nao ausencia
de jurisprudencia. O contrato anterior de JSF/ViewState permanece apenas como
evidencia historica e nao foi revalidado nesta rodada.
