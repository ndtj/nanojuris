# Research

The runtime provider already consumes the public TJPA BFF search envelope
`POST /bff/api/decisoes/buscar`. This change only versions sanitized parser
evidence for that existing contract. Detail routes remain unvalidated.
