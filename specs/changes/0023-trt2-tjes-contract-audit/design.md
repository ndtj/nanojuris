# Design

This is a non-promotional audit. Public GET requests and one bounded TRT2
document probe were inspected only to classify the source contract. No CAPTCHA,
challenge or WAF was bypassed. Because neither source currently exposes a
reproducible result contract, no runtime provider is introduced.
