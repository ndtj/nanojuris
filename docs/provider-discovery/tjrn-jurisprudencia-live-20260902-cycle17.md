# TJRN jurisprudência — rechecagem live bounded (2026-09-02)

Uma chamada pública, sem credenciais e sem persistir o corpo jurídico, respondeu
HTTP 200 com JSON e uma decisão para o termo jurídico de teste. O total remoto
declarado foi 73.401. O hash e os metadados estão no artefato JSON; o conteúdo
não foi armazenado.

Classificação: `reachable_valid_data` nesta janela. Isso não prova disponibilidade
permanente nem autorização de redistribuição. O adapter `tjrn_jurisprudencia`
continua opt-in e fora da busca federada até fechar contrato, estabilidade,
reuso e revisão humana.

Fonte: `POST https://jurisprudencia.tjrn.jus.br/api/pesquisar`.
